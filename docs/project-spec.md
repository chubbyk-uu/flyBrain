# 项目目标与实施规范

制定日期：2026-09-12；更新日期：2026-09-13。本文记录用户确认的目标和边界，是本 fork 后续开发的范围依据。
后续用户指示可更新本规范；计划中的能力不得表述为已经实现。

## 总体目标

基于 [mehrantsi/flyBrain](https://github.com/mehrantsi/flyBrain)，使用同一雄性标本的
MaleCNS v1.0 brain + VNC 数据，运行具有感官输入、神经计算和身体反馈的模拟果蝇。
起始代码版本为 `cdd3a127766ec184e19c4988fe12b6fd2cbc64fd`，开发分支为 `cuda-backend`。

原始 WebGPU 基线、独立 CUDA 核心及原生 MuJoCo 接入已经完成。
**当前最高优先级是全新室内场景、近源20秒寻食、绕盆脱困及网页运行体验修复。**
独立 Windows Three.js viewer 已接入，但新版场景视觉和长期自主行为尚未通过整体验收。
0.2 ms MuJoCo 物理步长已经通过 20 秒完整 CNS 闭环验收并达到实时，因而不再优先简化身体、关节或气动力。
身体简化方案只作为无法满足性能目标时的后备路线，不是当前实施计划。

目标表现：在更小的室内世界中探索，发现糖果后接近并舔食，飞到盆栽花朵的花蜜区域取食；
能够自由飞行、落地休息、到处爬行，避免长期原地转圈，并在合适条件下自主清洁。
饱食、疲劳、清洁冲动及随机探索允许由工程规则管理，不要求所有行为选择都从连接组自行涌现。
工程规则不能让 CNS 成为无作用的后台计算，亦不声称已经恢复真实果蝇的全部自主决策。
这些是目标而非已实现能力。各阶段分别记录与验收，不一次性大改。

当前连续实施与验收以
[全新室内场景与行为修复计划](indoor-repair-plan-2026-09-13.md)为准；
[旧六阶段路线](autonomous-fly-roadmap-2026-09-13.md)及其测试仅保留历史适用范围。历史优化依据见
[实时化、物理简化与行为完善计划](realtime-simplification-plan-2026-09-13.md)。
完整 CNS 在 0.2 ms 物理配置下已经达到至少 1× 实时；下一性能门槛为 Windows 浏览器
显示默认60 FPS（新计划规定平均≥55 FPS及帧间隔门槛）。两项继续独立测量，不能用仿真实时倍率代替显示 FPS。

本轮用户确认：环境从空房间新建，无地垫，低矮长茶几上放花盆和糖果，环境材质贴图全新；
无环境风，两个源共用一种食物气味；允许合法近源出生，以20仿真秒内实际进食验收。
后台启动即运行；每个后台进程仅在首次正式网页场景就绪时自动完整重置并继续运行，刷新、
重连及多标签不重复重置；手动重置后暂停。页面显示饥饿、疲劳和清洁冲动，不将其称为积灰度。
组胺/视觉前端补全及不同气味身份编码暂缓；当前视觉代理不能宣称识别糖果/花或高度。

连接组重建不等于完整复制动物。现有项目使用简化 LIF 神经元及工程化身体接口，不宣称
恢复供体记忆、身份或完整生理状态，也不将可见运动视为生物学行为验证。

## 原始基线与 CUDA 移植阶段的边界（历史范围）

- 保留现有 MaleCNS 数据、神经元 ID/顺序、CSR 拓扑、权重及其来源与哈希校验。
- 保留 `BrainBodyBridge` 的感官编码、probe 选择、滤波、sensory/motor mapping 和解码算法。
- 保留 MuJoCo 3.9.0 世界、NeuroMechFly 身体、碰撞、执行器、步态、翼运动、稳定控制和行为仲裁。
- 神经与物理步长保持 `0.1 ms`，默认每窗口 20 步，以 500 Hz 进行因果脑体交换。
- 仅替换 GPU 神经计算后端，允许必要的平台编译、动态链接、类型选择及验证入口适配。
- 保留 Metal 和浏览器 WebGPU 路径；不引入跨标本拼接、虚构连接或新的运动策略。
- 不通过减少神经元、删边、降采样或改变模型参数换取性能。
- 环境安装仅使用本项目独立 Conda 环境 `flybrain`，不再修改其他项目的共享环境。

上述世界、身体、映射及步长冻结约束适用于已完成的原始基线和 CUDA 移植/对照阶段，
不再作为实时简化阶段的全面禁令。新版允许独立调整物理步长、关节、碰撞、接触参数、
平均力飞行和身体接口；先保持神经 0.1 ms，再单独评估 0.2 ms 神经实验配置。
原始数组、神经元身份、连接与来源校验继续不变，不以删神经元或删边作为首选优化。
原资产和历史门控保留；新版配置、接口适配、假设和验收须版本化，不能覆盖基线记录。
环境仍仅使用独立 `flybrain` Conda 环境；下载沿用已配置代理，不修改其他项目环境。

## 数据与科学边界

上游导入模型包含 166,700 个神经元、24,469,412 条有符号有向边和 120,260,398 个
解剖接触。它不是原始连接表的全部分割片段：节点按 `superclass != null` 选择，
未知或未支持的递质效应对应的出边被显式省略；这些现有选择规则不得在移植中改变。

现有视觉通道在 MaleCNS 模式使用运动/接近代理输入，不是完整视网膜转导；食物搜索、
步态与飞行控制仍包含工程解码器。具体边界见
[CNS embodiment](cns-embodiment.md)、[odor guidance](cns-odor-guidance.md) 和
[pathway assay](male-cns-pathway.md)。原始数据来源见
[MaleCNS 官方下载页](https://male-cns.janelia.org/download/)。

现有舔糖、飞行、行走属于“神经活动驱动、工程控制器执行”的混合系统：

| 动作 | 神经活动参与 | 工程执行部分 |
|---|---|---|
| 舔糖/口器伸展 | 味觉输入经 CNS 传播，实际 MN9 脉冲参与伸展控制 | 接触到刺激频率、脉冲到关节角度、进食保持和离开时序 |
| 飞行 | 翼动力/转向运动神经元读出及相关活动门控 | 拍翼轨迹、姿态/速度稳定、升降和避障控制 |
| 行走 | 腿部运动神经元读出控制步态驱动/节奏 | 实测步态对应的关节轨迹及身体协调 |

这不等于每一下拍翼或每个关节轨迹都由连接组直接产生。本机后续原生 20 秒 intact 和
两个 12 秒断连对照已通过 21 项既有门控，见 [长程门控报告](native-long-behavior-gates-2026-09-12.md)。
这不证明视觉识别糖/花或数分钟自主行为已经成立。现有取食仍采用约 3 秒进食、12 秒
味觉抑制的历史状态机；后续已加入homeostasis及清洁控制，但其与旧仲裁的组合仍有待修复，
不能视为近源20秒寻食和防长期循环已通过。

## 运行路线

| 路线 | 组成 | 当前状态 |
|---|---|---|
| Windows 浏览器 | Rust/MuJoCo WASM + WebGPU 神经计算 + 浏览器显示 | Windows Chrome/RTX 5080 完整 CNS 基线已通过，Edge 未单独测量 |
| macOS 原生 | Rust + Metal + MuJoCo/GLFW | 保留上游路径 |
| Linux/WSL2 原生 | 原生 Rust + CUDA + 原生 MuJoCo + 同一世界/身体资产 | 已接入并通过基础及长程行为门控 |

浏览器优先使用 Windows 侧 Chrome/Edge；WSL 可只运行本地静态服务器。
浏览器版不需要 CUDA Toolkit。源码重建才需要上游固定的 Emscripten 4.0.10 和
MuJoCo 3.9.0 源码，详见 [browser runtime](browser.md)。

CUDA 路线不得把 MuJoCo 留在浏览器或 WASM 中。WebGPU/WASM 路线只作为已校验基线、
演示和跨平台参考保留；正式 WSL/Linux 闭环必须由原生 Rust 进程同时承载 `CudaEngine`、
原生 MuJoCo 和后续显示层。第一版 CUDA 核心仍按隔离原则不接世界，核心验收完成后才
接入原生 MuJoCo；该历史阶段使用相同 MJCF、身体资产、世界状态与 `BrainBodyBridge` 语义。
新的实时配置继续采用原生路线；当前保留完整身体，已研究的身体和接口简化仅作为后备。

仓库提交了 WASM 和运行资源，但 `.gitignore` 排除了 `outputs/packs/`。
新克隆必须先恢复 `outputs/packs/male_cns_v1`；本机已恢复并校验。
不能把 `npm start` 成功当作完整 CNS 成功。
优先获取与现有 I/O 哈希一致的作者数据包；线上大数组分块需顺序重组并校验原始完整哈希。
若改从官方 Feather 重建，必须使用既有导入器和规则，验证四个数组与 I/O 绑定一致。
不得重新生成 I/O 或更换数据来绕过不匹配。

## 最小 CUDA 设计（已完成阶段的设计依据）

首步只新增独立 `CudaEngine` 与测试/诊断入口，不接 MuJoCo/world，不修改
`BrainBodyBridge` 的后端选择。后续接入世界时才沿用现成的 `NeuralEngine` 类型别名，
添加 Linux/CUDA 分支，不为此引入通用后端框架。核心兼容构造参数、`run_window_sparse()`、
窗口 `elapsed`/`spike_count_deltas` 和设备、内存、群体统计接口；离线验证需要
`run_schedule()`、`run_recorded()` 等对应能力。

逐项移植 `flybrain.metal` 的七个内核：`decay_threshold`、`propagate_csr`、
`decay_threshold_propagate_delayed`、`reset_decay_threshold`、`apply_external`、
`apply_external_sparse`、`reset_store`。保持 f32 状态、i16 权重、i32 原子累加、
严格阈值、延迟环、不应期、静默源和窗口边界顺序。特别核对零延迟和非零延迟分支。

CSR 和神经状态常驻设备显存。使用有序 CUDA stream 批量提交窗口，上传稀疏事件并
集中读回 probe；保留遥测语义。不得照搬 Metal 共享内存的主机直接解引用方式。
首版以一致性为主，不同时引入新的稀疏传播算法。是否使用 NVRTC、CUDA Graph 或额外
归约内核，依据接口验证与测量决定，不作为首版前置重构要求。

## 历史实施顺序

以下保留移植至原六阶段期间的实施记录；序号内“当前/后续”是历史状态。
当前修复顺序及验收以[全新室内修复计划](indoor-repair-plan-2026-09-13.md)为准，
不再先做身体简化或继续沿用旧场景拼装。

1. 原始基线（已完成）：独立环境、MaleCNS/资源校验、Windows Chrome/RTX 5080
   WebGPU QA、20 秒带渲染完整 CNS、重置检查；见 [实测报告](baseline-rtx5080-windows.md)。
2. 性能诊断（已完成）：GPU timestamps、无观察者和 world-only 对照表明，WebGPU
   compute pass 平均约 `0.506 ms/窗口`，但同步式浏览器 neural-engine 路径约
   `4.000 ms/窗口`；world-only 的 WASM 物理仍只有 `0.301×`。详见实测报告。
3. 独立 CUDA 神经核心（已完成）：Rust/CUDA 核心、tiny fixture、自身确定性、
   chunk/sparse/split-window parity 已通过；完整 MaleCNS 的 15 个固定输入 case、关键群体
   和既有实验门控也已通过预注册验收。见 [CUDA 核心阶段报告](cuda-engine-stage-2026-09-12.md)
   与 [完整 CNS CUDA 报告](cuda-full-cns-stage-2026-09-12.md)。
4. 原生世界接入与实验回归（已完成）：保持原世界/映射，CUDA 已接入原生 MuJoCo，
   不经过浏览器/WASM；2 秒 `cns-check` 成对门控、无感官输入、运动/嗅觉断连和
   WSLg viewer 已通过。bridge 已改为设备端 probe/telemetry 小读回并复用 CUDA 临时
   缓冲区；固定 probe 也已跨窗口缓存，50 ms profile 的 CUDA copy 调用从 154 次降至
   92 次。固定 0.2 秒闭环曾测得约 `0.294×` 实时，但 1 秒复测仅约 `0.184×`，证明
   低活动短测不能代表持续性能。CUDA 已默认使用保持原 i32 arrival 和 tick 顺序的
   256-edge chunked CSR propagation；1 秒复测的 neural engine 达到约 `2.40×` 实时，
   完整闭环达到约 `0.800×`，且完整 CNS 与 world gate 均保持一致。重新 profile 后，
   1 秒窗口中 physics/flight loop 中位数约 `0.754 s`、brain wall 约 `0.513 s`，其余
   bridge/控制约 `0.009 s`。保持 tick 顺序的 CUDA Graph 实验在 5 秒测试中仅令 neural
   engine 中位数快约 `4.2%`，完整闭环没有改善，因此保留为 opt-in、继续以 direct 为
   默认。20 秒 intact 及两个 12 秒断连对照已通过既有 21 项取食/嗅觉门控；重复 intact
   的全部非计时输出一致。原生 physics/flight 分项也已完成：三次 5 秒 headless 运行的
   `mj_step` 中位数为 `3.192 s`，占物理时间 `95.66%`；完整闭环约 `0.835×` 实时。
   profile 开/关的一秒报告在剔除计时字段后全部输出一致。随后先进行了保持 timestep、
   世界、身体和控制语义的 MuJoCo 单变量 A/B。第一项 A/B 已默认关闭
   未被项目读取的 MuJoCo energy 统计：六组交替测试的 `mj_step` 中位数改善 `2.83%`，
   20 秒 intact 与原基线的全部神经/行为/轨迹输出一致，21/21 长程门控再次通过；可用
   `FLYBRAIN_MUJOCO_ENERGY=1` 恢复原诊断。见
   [原生接入报告](native-cuda-mujoco-stage-2026-09-12.md)与
   [CUDA 闭环传输优化报告](cuda-native-transfer-optimization-stage-2026-09-12.md)、
   [probe 缓存与瓶颈复测报告](cuda-native-probe-cache-stage-2026-09-12.md)、
   [chunked CSR propagation 报告](cuda-chunked-propagation-stage-2026-09-12.md)、
   [原生闭环组件分项报告](native-closed-loop-component-profile-2026-09-12.md)、
   [CUDA Graph 实验报告](cuda-graph-experiment-2026-09-12.md)、
   [原生长程行为门控报告](native-long-behavior-gates-2026-09-12.md)、
   [原生 MuJoCo 物理分项报告](native-physics-profile-2026-09-12.md)、
   [MuJoCo energy 统计优化报告](native-mujoco-energy-optimization-2026-09-12.md)。
   原生 viewer 已拆为模拟后台线程和主线程独立绘制，固定输入短程状态对照通过；窗口分别
   显示实际 FPS 和仿真倍率。WSLg 实测仍存在绘制瓶颈：默认阴影约 3 FPS，可选关闭观察者及
   双眼阴影的短测约 13 FPS，尚未达到流畅显示；用户持续运行报告约 6–7 FPS，不能用短测
   代替长期表现。见 [viewer 解耦报告](native-viewer-decoupling-2026-09-12.md)。
   **2026-09-12 优先级调整（用户决定）：真实仿真运行速率为当前最高优先级。**
   先依据官方资料和本机 profile 优化 CUDA + 原生 MuJoCo 闭环吞吐，不先推进新行为/场景。
   显示 FPS 单独记录，不把插帧、少显示或慢放当作生物时间加速。原先先排查等价优化；
   2026-09-13 按用户新目标转入独立轻量配置，允许身体/物理近似，但不能暗中改参考模型。
   待办（非优先）：用户报告运行约 300 秒后长期飞行转圈；代码存在持续巡航、食物/接触
   降落门控和避障覆盖转向路径，但本次具体循环原因尚无连续遥测证实。现有约 20 秒门控
   不代表长期自主行为验收，不用定时强制落地掩盖问题，留待速率优化后诊断。
5. 原生实时配置（当前）：MaleCNS 神经仍为 0.1 ms，MuJoCo 物理采用 0.2 ms 默认；
   保留完整 132 DOF 身体。20 秒闭环完成约 2.998 秒取食、餐后离开并达到约 1.065×。
6. Viewer 优化（当前最高优先级）：停止优化 WSLg classic OpenGL 主 viewer。原生进程以
   WebSocket 20–30 Hz 发布 body poses 和 snapshot，复用既有 Three.js renderer 在 Windows
   Chrome/Edge 中用 `requestAnimationFrame` 独立绘制并插值。阶段 1 浏览器严格只负责显示；
   native MuJoCo 双眼 retina/readback 与 sensory 输入保持原路径。行为与 20–30 FPS 验收后，
   才单独评估浏览器 binocular retina 异步 readback，不能在阶段 1 混入。
7. 混合行为（后续）：饥饿/饱食、疲劳/休息、单一积灰与合并搓腿—头眼清洁、持续随机
   探索和防绕圈机制。行为完成后再设计更小的茶几、糖果和开花盆栽室内场景及果蝇外观。
8. 身体简化（后备）：只有完整身体在上述显示优化后仍无法满足总体性能目标时才恢复评估。

阶段 A–F 的内容、文件和验收见 [详细计划](realtime-simplification-plan-2026-09-13.md)。

每阶段完成后先汇报结果，再继续下一阶段。性能均分别报告加载、神经计算/传输、
桥接、物理与显示开销；不承诺 RTX 5080 必达实时，不把上游 M3 Max 测量外推成本机结果。

## CUDA 验收标准

不要求 CUDA 与 CPU-f64 长时间逐神经元完全一致。验收分三层：

1. CUDA 自身确定性：同设备、同构建、同初态和同固定输入重复运行结果一致；
   改变窗口划分不得改变结果。墙钟驱动的视网膜输入不是固定输入，不用于直接证明
   长期闭环逐位确定性；如需对比，先固定并回放感官事件。
2. 短程正确性：tiny fixture 的 spike、延迟环、不应期、严格阈值、抑制、静默源、
   执行顺序和稠密/稀疏输入等价正确，覆盖跨窗口及高扇出情况。
   f32 状态使用预先定义的容差。报告首次非零数值差异、首次超容差和首次 spike
   时序分歧的 tick/神经元/状态；未发生分歧时说明测试范围，不把正常舍入差异判为失败。
3. 完整 CNS 实验一致性：固定刺激下比较 population response，并检查关键感官、DN、
   运动群体的响应与已有门控/断连效应，不能只比较全脑平均值。
   指标、观察窗口、对照条件和容差在验收前确定。独立神经实验与接入世界后的行为门控
   分开执行；移植回归使用同一原始世界和映射。新版轻量身体允许轨迹及感觉反馈变化，
   使用预先确定的功能/因果门控，不要求与旧闭环长期逐样本一致。

CPU-f64 用于短程语义参考和差异诊断，既有 f32/FMA CPU 诊断用于区分精度效应与
调度实现错误。长程 f32/f64 偏离本身不判定 CUDA 失败。

数值验证必须保留上游已知失败：100 ms MaleCNS CPU-f64/Metal 的严格 `0.001 mV`
最终状态门槛失败，MeVP24–DNp10 候选通路的生物学验收也失败。
这两项仍作为历史失败保留，不把严格长程 f64 逐神经元状态门槛当作 CUDA 的新验收要求，
也不要求 CUDA 将已失败的生物学假说“跑成通过”。不能仅凭累计计数一致宣称逐步一致，
不得事后放宽原门槛、修改历史结论或调行为参数掩盖移植错误。

## 后续场景、外观与翼运动需求

用户指定从空场景全新建立小室内环境，低矮长茶几上放糖果和带花盆栽，无地垫，材质贴图重做，
提供可落脚、探索、清洁和取食的位置。尺寸、近源出生及逐视角验收见当前修复计划。
同时改善果蝇外观；新场景作为独立配置/资产版本保留，原始基线仍能重现。

气味与接触味觉分开建模：纯糖不作为远距离气味源；带香味的糖果及花朵可作为后续
嗅觉刺激来源。首版确认两源共用一种气味、同释放参数、无风、三维浓度叠加；不因摆放花朵就假定
MaleCNS 已具备相应花香识别，也不让控制器直接读取食物坐标来替代感官导航。

用户认为现有翅膀运动看起来不对，这是待诊断的问题，尚未证实运动算法有误。
先区分显示帧率/采样混叠、翼关节坐标与轨迹、拍翼相位及物理飞行控制问题。
视觉修正与真实运动/控制修正分开验证；如涉及后者，记录关节运动、支撑/升力及稳定性
回归，不能在 CUDA 对照中同时改变拍翼算法。

## 需求状态与合并清洁（用户确认的新目标）

- 饱食度：实际接触食物且伸吻取食时增加，随时间及活动消耗下降；吃饱停止取食，饿了再觅食。
- 飞行疲劳：飞行及低速/零水平速度有功悬停均累积，落地稳定支撑后爬行或静止均恢复；
  正常飞行约30–45秒达到疲劳门槛，落地约10秒恢复起飞资格，不强制全程静止。
  疲劳高先寻找支撑并降落，不在空中直接关闭翅膀；这些新节奏待实施验收。
- 饱食后的探索：爬行和飞行允许工程化随机方向，保持一段方向再平滑改变，并结合避障和
  停滞/重复绕圈检测；不能每帧随机转向，也不保证一切弯曲轨迹都应消除。
- **仅一个清洁冲动值、一套清洁动作**：这是习惯性梳理的工程倾向，不代表真实灰尘；随时间累积，
  地板或茶几上的爬行/探索状态下、不饿、
  稳定着地且达到阈值时可以触发。先停止移动、稳定支撑，再执行前足搓擦及擦拭头部/复眼。
  不区分腿部与头部变量，不拆为两个独立行为。动作实际执行才降低冲动，设置冷却和可复现随机性，
  饥饿/危险可中断。旧实现dirt字段的迁移须显式说明。
  初次清洁机会约10–15仿真秒，完成后随机10–20秒再次出现机会，动作2–3秒、至少5秒冷却；
  不安全或饥饿时推迟，不能连续补做，固定种子可复现。

长程验收为5个固定种子各300仿真秒：每次必须实际取食糖果和花蜜、包含有效飞行/爬行和
支撑停留，并检查自主清洁。既不得固定动作表或局部绕圈反复执行，也不得长期无有效活动。
动作标签/次数不够，须检查物理动作、状态变化、空间进展和对感觉变化的响应，具体门控见
[当前修复计划](indoor-repair-plan-2026-09-13.md)。

工程规则允许管理需求、触发行为、选择探索方向及协调动作，不要求清洁意图必须从原始
连接组涌现。仍须明确每种动作的 CNS 门控/调节位置，并用运动断连等对照验证，不能将
已有姿态控制器改名后宣称恢复了真实清洁神经回路。特定通路如未查证，标记为工程假设。
建议用连续状态、开始/结束不同阈值及最短保持时间避免状态抖动；优先级建议为安全与
支撑 → 疲劳降落/休息 → 饥饿觅食 → 清洁 → 探索，具体阈值和冲突处理留待阶段 F 校准。

已有需求状态和统一清洁代码；清洁冲动语义及新版门控待实现/复验。原诊断背景见 [neural grooming](neural-grooming.md)。

## 原 CUDA 阶段文件清单（历史设计）

以下保留原 CUDA 及原世界接入时的设计范围，阶段实际结果以上述报告为准，不代表当前待办。
新的身体、物理、显示及行为改动清单见 [实时简化计划](realtime-simplification-plan-2026-09-13.md)。

| 文件 | 改动目的 |
|---|---|
| 新增 `rust/src/cuda_engine.rs` | CUDA 状态、调度、传输、读回和兼容 API |
| 新增 `rust/cuda/flybrain_cuda.cu` | 神经内核与 CUDA runtime 边界 |
| `Cargo.toml`、`build.rs` | 可选 CUDA feature 与 `nvcc` 构建；Linux MuJoCo 依赖留待后续 |
| `rust/src/lib.rs` | 模块的平台条件编译 |
| `rust/src/brain_bridge.rs` | 仅后端 import/type alias |
| `rust/src/main.rs`、`output.rs`、`bin/cns_numerics.rs` | 消除验证入口硬编码的 Metal 类型依赖 |
| `rust/src/live_viewer.rs` | GLFW 平台链接名称 |
| `.cargo/config.toml`、`tools/setup_mujoco_runtime.py` | Linux 动态库、搜索路径和运行库准备 |
| 新增 CUDA 验证测试和运行说明 | 可复现验证与性能记录 |

原移植阶段仅允许必要的平台适配，不改变世界和桥接算法。当前新版允许显式的身体接口
适配，仍保留原数据、I/O 绑定和历史 Metal/WebGPU 基线，不跨标本拼接或虚构神经连接。

## 状态管理

当前状态以[修复计划](indoor-repair-plan-2026-09-13.md)为准：场景视觉检查已发现缺陷，
用户长程绕盆问题未解决；需要新验收，不能宣称整体完成。以下为历史移植/性能阶段记录，
其中“当前”“后续”及优先级只指各记录当时，不覆盖本轮方案。

当前状态：阶段 1、性能诊断、独立 CUDA 核心及完整 CNS 固定回放验收已完成，见
[Windows/RTX 5080 基线](baseline-rtx5080-windows.md)和
[CUDA 核心阶段报告](cuda-engine-stage-2026-09-12.md)、
[完整 CNS CUDA 报告](cuda-full-cns-stage-2026-09-12.md)。
2026-09-13 当前后续顺序以阶段 A–F 为准：先真实吞吐与轻量身体，再显示/场景和混合行为；
0.2 ms 神经步长只作为身体优化后的独立候选，不直接替换默认值。
当前 CUDA 已接 `BrainBodyBridge` 和原生 MuJoCo/world；2 秒成对门控通过，且没有修改
数据、映射、行为或模型参数。设备端 probe、缓冲复用、probe 缓存和 chunked propagation
已经完成；5 秒 direct 闭环中位数约 `0.828×` 实时。CUDA Graph 仅小幅改善神经侧，
不设为默认。20 秒取食/嗅觉及断连门控已通过，重复运行的非计时输出完全一致；长程
intact 约 `0.63–0.67×` 实时，主要新增开销在 physics/flight loop。后续仍只使用原生
MuJoCo，不使用浏览器/WASM 物理。
本机检查与环境恢复记录见 [环境检查记录](environment-check-2026-09-12.md)。
新轻量身体、时间步分离、平均力飞行、需求状态和 0.2 ms 神经实验均未完成；已有运行时
开关和本机编译探索不等于这些阶段已实现。300 秒转圈仍为用户报告的未解决问题，非当前
首要修复项，但新版长期行为验收必须覆盖。本文更新不修改代码或默认配置。
历史上游测试结果以原文档为准，不标为本机通过。后续阶段结果单独记录日期、版本、
输入哈希、命令及通过/失败/未执行状态，再更新本规范的进度描述。
